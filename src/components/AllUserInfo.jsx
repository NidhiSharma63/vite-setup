import useFetchUsers from "../utils/useFetchUser";

const AllUserInfo = () => {
  // const { users } = useFetchUsers();
  return (
    <>
      <h1>All User Info</h1>
      {/* {users?.map((user) => {
        return (
          <>
            <p>{user.name}</p>
            <p>{user.id}</p>
          </>
        );
      })} */}
    </>
  );
};

export default AllUserInfo;
